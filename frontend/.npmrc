# CPDraw frontend (WO-0.1)
# Fail rather than silently run under an unpinned Node — this project's build
# is deliberately held to the Vite 5 / Svelte 4 line that Annotorious v3 uses.
engine-strict=true

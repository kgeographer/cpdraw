# Session handoff — after WO-0.5 (2026-09-03)

Read `CLAUDE.md` first, then this. Detail + gotchas for the last session are in
`logs/session_log_20260903.md` (gitignored, local only — read it).

## State

- **Phase 0 complete** (WO-0.1 → WO-0.5). `main` @ `4f1b196`, pushed. 103 tests
  pass, `manage.py check` / `pnpm check` / `pnpm build` clean.
- Local-only branch `wo-0.5` is fully merged — delete it whenever (`git branch -d wo-0.5`).
- Dev = two processes: `python manage.py runserver` + `pnpm --dir frontend dev`.
  Non-interactive shell needs `export PATH="/opt/homebrew/opt/node@22/bin:$PATH"`.
- Dev DB: `galicia` (admin, 4 sources) + `gascooking` (Satch, has scope). Only
  `admin` is superuser; no `karlg` user yet.

## Next — WO-0.6

Scope is listed in `docs/WO_0.5.md` §8 (carried-forward) and §9 (as-built /
follow-ups). In rough priority:

1. **Join keys / invite flow** — `ProjectInvite` model, `/join/<key>/`, owner
   mint/revoke UI. This is what makes editors/annotators reachable at all.
2. **Object-level DRF permissions** — rebuild `accounts/permissions.py` (deleted
   in WO-0.5) as project-role-aware classes consuming `Project.can_*`; apply to
   `/api/annotations/` + `/api/project-placetypes/`.
3. **`MapImagePlacetype` read path + editor UI** — table + `available_placetypes`
   shipped in WO-0.5; wire the annotation picker to it and add the per-map
   subset toggle.
4. Front-end library modernization (Bootstrap 4→5, drop jQuery/jQuery-UI, fix the
   `http://ajax.googleapis.com` mixed-content link).
5. Read-only Source detail page (retire the `/admin/` links).

Small follow-ups: `Profile.web_page` should get `blank=True` (form has a
stopgap); confirm `fetch_projects` endpoint is still used or drop it.

**First step: write `docs/WO_0.6.md`** (same shape as WO_0.2–0.5), discuss scope
with Karl before building. Karl asks → propose first; don't commit UI-facing work
until he's seen it in the browser; he built LPF/WHG — defer on gazetteer/format
design.
